# Running View

This directory is reserved for experiments that are actively running right now.

Suggested usage:

- keep the canonical experiment directory in `../by_id/`
- add a symlink here only while the process is live
- remove the symlink when the run finishes

This keeps the archive stable while giving you a clean operational view.
