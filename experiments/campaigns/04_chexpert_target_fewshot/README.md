# 04 CheXpert Target Few-Shot

This campaign contains the target-only `CheXpert` few-shot experiments using frozen `CXR Foundation` features.

Included here:

- target embedding exports
- linear probe training runs
- `250`, `500`, and `1000` target-shot settings

Primary range:

- `exp0050` to `exp0055`
