# 06 MIMIC Target Scaling And Adaptation

This campaign contains the later `MIMIC` follow-up runs after the initial few-shot probes failed.

Included here:

- larger target-train split variants
- frozen linear and MLP comparisons on the same split
- backbone-adaptation feasibility check follow-up
- image-level partial-finetuning fallback

Primary range:

- `exp0065` to `exp0073`
