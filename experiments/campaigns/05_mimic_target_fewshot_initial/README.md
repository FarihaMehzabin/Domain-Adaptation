# 05 MIMIC Target Few-Shot Initial

This campaign contains the first target-only `MIMIC` few-shot runs and their corrected reruns.

Included here:

- initial `MIMIC` target embedding exports
- initial linear probe runs
- corrected reruns after the local path/layout fix

Primary range:

- `exp0056` to `exp0064`
