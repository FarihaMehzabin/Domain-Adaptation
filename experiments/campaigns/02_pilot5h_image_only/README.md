# 02 Pilot5H Image Only

This campaign contains the pilot image-only domain-transfer runs on the `pilot5h` shared-label setup.

Included here:

- ResNet50 batch-size and embedding export runs
- frozen linear probes
- source-trained MLP sweep
- image-only ViT LoRA pilot

Primary range:

- `exp0011` to `exp0027`
